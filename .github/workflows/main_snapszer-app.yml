# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

#name: Build and deploy Node.js app to Azure Web App - snapszer-app
#
#on:
#  push:
#    branches:
#      - main
#  workflow_dispatch:
#
#jobs:
#  build:
#    runs-on: ubuntu-latest
#    permissions:
#      contents: read
#
#    steps:
#      - uses: actions/checkout@v4
#
#      - name: Set up Node.js version
#        uses: actions/setup-node@v3
#        with:
#          node-version: '22.x'
#
#      - name: npm install, build, and test
#        run: |
#          # Fő könyvtár függőségei
#          npm install
#          
#          # Kliens build folyamat
#          if [ -d "client" ]; then
#            cd client
#            npm install
#            npm run build
#            cd ..
#          fi
#          
#          # Tesztek és egyéb build lépések a gyökérben (ha vannak)
#          npm run build --if-present
#          npm run test --if-present
#
#      - name: Upload artifact for deployment job
#        uses: actions/upload-artifact@v4
#        with:
#          name: node-app
#          # A pont (.) biztosítja, hogy a teljes projektstruktúra, 
#          # benne a frissen generált client/dist is bekerüljön a csomagba.
#          path: .
#
#  deploy:
#    runs-on: ubuntu-latest
#    needs: build
#    environment:
#      name: 'Production'
#      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}
#    
#    steps:
#      - name: Download artifact from build job
#        uses: actions/download-artifact@v4
#        with:
#          name: node-app
#      
#      - name: 'Deploy to Azure Web App'
#        id: deploy-to-webapp
#        uses: azure/webapps-deploy@v3
#        with:
#          app-name: 'snapszer-app'
#          slot-name: 'Production'
#          package: .
#          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_70BF6B819EA14DA5A08C6E34F4B554D9 }}